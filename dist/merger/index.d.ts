/**
 * Merges CSS class names by removing empty strings and duplicates.
 *
 * @param classes Array of class names to merge.
 * @returns Normalized class string, or an empty string when no classes remain.
 */
export declare function classMerger(classes: ReadonlyArray<string>): string;

export {};
